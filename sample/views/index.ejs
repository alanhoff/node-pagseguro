
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <meta name="description" content="">
    <meta name="author" content="">

    <title>node-pagseguro sample</title>
    
    <link href="css/styles.css" rel="stylesheet">
    <script type="text/javascript" src="js/lib/angular/angular.js"></script>
    <script type="text/javascript" src="js/app.js"></script>
    <script type="text/javascript" src="js/controllers.js"></script>
  </head>

  <body ng-app="app" ng-controller="HomeCrtl">

    <nav class="navbar navbar-inverse navbar-fixed-top">
      <div class="container">
        <div class="nav-title">
          <h1>node-pagseguro sample</h1>
        </div>
        <div class="shopcar" > 
          <p><strong>Total</strong><p>
          <p>R$ {{total}}<p>
          <p><strong>Items</strong><p>
          <p>{{count}}<p>
          
          <input type="button" data-ng-click="checkOut()" value="finalizar compra" />
        </div>
      </div>
    </nav>

    <div class="container">
     
      <div class="row">
          <div class="col-md-4" ng-repeat='item in items'>
              <h3>{{ item.name }}</h3>
              <p>R$ {{ item.price }}</p>
              <img ng-src="{{ item.imageUrl }}" alt="" width="130" />
              <p><a class="btn btn-default" data-ng-click="addItem(item)" href="javascript:void(0);" role="button">adicionar</a></p>
          </div>
      </div>

      <hr>

      <footer>
        <br />
        <p>T-Shirt Shop - 2015</p>
      </footer>

    </div> 

 </body>
</html>
